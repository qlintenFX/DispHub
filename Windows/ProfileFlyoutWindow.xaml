<Window x:Class="DispHub.Windows.ProfileFlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="48" Width="200"
    ResizeMode="NoResize"
    WindowStyle="None" ShowInTaskbar="False"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    Loaded="Window_Loaded">

    <Border x:Name="MainBorder" CornerRadius="8" Margin="4"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1">
        <Border.Background>
            <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" Opacity="0.92"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.25" BlurRadius="12" ShadowDepth="2"/>
        </Border.Effect>
        <TextBlock x:Name="ProfileNameText" Text="Default"
                   FontSize="14" FontWeight="Medium"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis" MaxWidth="175"/>
    </Border>
</Window>
